# ATM Face Biometric Authentication System
# Flask Web Application — Requirements
# Python 3.10+

# Web Framework
flask>=2.3.0

# Deep Learning Face Recognition (dlib ResNet-34 — 128d embeddings)
face_recognition>=1.3.0
dlib>=19.24.0       # compiles ~5-10 min; or use pre-built wheel

# Computer Vision
opencv-python>=4.8.0

# Numerical Computing
numpy>=1.24.0

# Image Processing
Pillow>=10.0.0

# Standard library only (no extra install needed):
# sqlite3, base64, pickle, hashlib, os, datetime
